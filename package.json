{
  "name": "portfolio-v2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "bgd": "yarn build && yarn generate && yarn deploy"
  },
  "dependencies": {
    "@nuxt/content": "^1.14.0",
    "@nuxtjs/axios": "^5.12.0",
    "nuxt": "^2.14.0",
    "nuxt-purgecss": "^1.0.0",
    "push-dir": "^0.4.1"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^4.0.0"
  }
}
