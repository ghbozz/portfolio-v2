{
  "name": "portfolio-v2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "gd": "yarn generate && yarn deploy"
  },
  "dependencies": {
    "@nuxt/content": "^1.14.0",
    "@nuxtjs/axios": "^5.12.0",
    "@nuxtjs/tailwindcss": "^4.1.3",
    "nuxt": "^2.14.0",
    "push-dir": "^0.4.1"
  },
  "devDependencies": {
    "node-sass": "^6.0.0",
    "sass-loader": "^12.1.0"
  }
}
